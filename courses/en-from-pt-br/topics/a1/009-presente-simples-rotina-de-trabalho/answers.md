# Gabarito: Presente simples: rotina de trabalho

Nível: A1

## Gabarito dos exercícios

| ID | Resposta | Explicação |
|---|---|---|
| 1.1 | I write code. | Com `I`, o verbo fica na forma básica: `write`. |
| 1.2 | I don't use Windows. | A negativa com `I` usa `don't` antes do verbo principal. |
| 1.3 | Do you test the app? | Perguntas com `you` no presente simples usam `do`. |
| 1.4 | She works from home. | Em afirmativas com `she`, normalmente acrescentamos `s` ao verbo. |
| 1.5 | Does he deploy the app? | Com `he`, a pergunta usa `does` e o verbo principal fica `deploy`. |
| 1.6 | Yes, I do. | A resposta curta para pergunta com `Do you...?` usa `I do`. |
| 2.1 | don't | Com `I`, a negativa usa `don't`. |
| 2.2 | Do | Pergunta com `you` usa `Do` no início. |
| 2.3 | doesn't | Com `she`, a negativa usa `doesn't`; o verbo fica `test`. |
| 2.4 | Does | Pergunta com `he` usa `Does` no início. |
| 2.5 | don't | Com `we`, a negativa usa `don't`. |
| 3.1 | I work with software. | Para rotina com `I`, use `work` na forma básica. |
| 3.2 | We test the app after lunch. | Com `we`, o verbo fica `test`; `after lunch` indica depois do almoço. |
| 3.3 | I don't deploy on Friday. | A negativa com `I` usa `don't` antes de `deploy`. |
| 3.4 | Do you use Linux? | Perguntas com `you` usam `Do you...?`. |
| 3.5 | She writes code in the morning. | Com `she`, `write` vira `writes` na afirmativa. |
| 4.1 | I don't use Windows. | Para negar `I use`, acrescente `don't` antes do verbo. |
| 4.2 | Do you test the app? | Para transformar `You test...` em pergunta, coloque `Do` no início. |
| 4.3 | Does she deploy the app? | Com `she`, use `Does`; o verbo principal não recebe `s`. |
| 4.4 | He doesn't write code. | Com `he`, a negativa usa `doesn't` e o verbo volta para `write`. |
| 4.5 | We check tickets every day. | `Every day` pode ir no fim para marcar rotina diária. |
| 5.1 | I work from home. | Resposta-modelo: frase afirmativa simples com `work`. |
| 5.2 | I don't use Windows. | Resposta-modelo: negativa com `don't` e uma ferramenta ou sistema. |
| 5.3 | Do you use Git? | Resposta-modelo: pergunta direta com `Do you...?`. |

## Gabarito do teste

| ID | Resposta | Explicação |
|---|---|---|
| 1 | I fix bugs every day. | Com `I`, use `fix`; `every day` indica rotina. |
| 2 | It runs on Linux. | Com `it`, o verbo recebe `s` na afirmativa: `runs`. |
| 3 | Does she join the daily? | Pergunta com `she` usa `Does`; o verbo principal fica `join`. |
| 4 | They don't use that repository. | Com `they`, a negativa usa `don't`. |
| 5 | Yes, he does. | Resposta curta para `Does he...?` usa `he does`. |
| 6 | Do you deploy today? | Pergunta com `you` usa `Do you...?`. |
| 7 | writes | `My team` é singular aqui; por isso, `write` vira `writes`. |
| 8 | Does | `Your manager` equivale a `he` ou `she`; a pergunta usa `Does`. |
| 9 | I don't test in production. | A negativa com `I` usa `don't`; `test` fica na forma básica. |
| 10 | We read tickets in the morning. | Com `we`, use `read` sem `s`; `in the morning` indica de manhã. |
| 11 | Does she check the dashboard every day? | Em pergunta com `she`, use `Does` e mantenha `check` sem `s`. |
| 12 | Yes, I do. | Resposta-modelo curta e correta para `Do you work with software?`. |

## Respostas da história

| Pergunta | Resposta | Observação |
|---|---|---|
| 1 | She writes code in the morning. | A história diz: `In the morning, I write code.` |
| 2 | No, she doesn't. | Ana diz: `I don't use Windows at work.` |
| 3 | Leo checks the dashboard. | A frase da história é `My coworker Leo checks the dashboard.` |
| 4 | No, he doesn't. | A história informa que Leo não faz deploy na sexta-feira. |
| 5 | They deploy a small fix. | No fim da rotina, a equipe faz deploy de uma pequena correção. |
